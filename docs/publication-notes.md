# Publication Notes

The repository was prepared from the delivered MATLAB implementation folder.

The following items are intentionally excluded:

- compressed simulation archives (`.zip`, `.rar`, `.7z`);
- temporary MATLAB files;
- generated local outputs;
- raw/private data folders.

Sample `.mat` datasets are included because they are needed to reproduce the MATLAB workflow.
